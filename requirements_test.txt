-r requirements.txt

# Runtime dependency of homeassistant.components.mqtt, which
# custom_components.open3e imports at module load. `pip install homeassistant`
# does not pull in per-integration manifest requirements, so it is listed
# explicitly for tests. The pin mirrors homeassistant/components/mqtt/
# manifest.json (paho-mqtt==2.1.0 in HA 2026.6.4) - re-check it whenever the
# homeassistant pin in requirements.txt changes.
paho-mqtt==2.1.0
